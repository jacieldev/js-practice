//Escribir una función que reciba un número y devuelva “par” o “imp
const isParOrImp = (number) => number % 2 == 0 ? "El numero es par" : "El numero es impar"

console.log(isParOrImp(5))
console.log(isParOrImp(4))